# Variables de entorno de Supabase (copia este archivo a .env y rellena los valores).
# Son claves públicas (PUBLIC_*), se inyectan en el cliente durante el build.
PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_ANON_KEY=
